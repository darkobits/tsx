{
  // esbuild (used in Vite) doesn't seem to use the same logic for resolving
  // tsconfig files using bare package names, so we need to provide a package
  // name and path here instead.
  "extends": "@darkobits/ts/tsconfig.json",
  "compilerOptions": {
    "plugins": [
      {"name": "typescript-styled-plugin"},
      {"transform": "@zerollup/ts-transform-paths"}
    ],
    "jsx": "react",
    "lib": [
      "DOM",
      "DOM.Iterable",
      "ESNext"
    ],
    "types": [
      "node",
      "vite/client",
      // Unfortunately, Typescript does not merge "types" from parent
      // configurations. Therefore, although this is not one of our
      // dependencies, this is included here so that consumers who use this
      // plugin do not have to re-declare "types" => "vite/client" in their
      // configuration.
      "vite-plugin-pwa/client",
      "vite-plugin-svgr/client"
    ]
  }
}
